dependencies:
  override:
    - npm install
    - cd ./serrano-library && pwd && npm install
    - cd ./simple-extension && pwd && npm install
  cache_directories:
    - "node_modules"
    - "serrano-library/node_modules"
    - "simple-extension/node-modules"

test:
  override:
    - grunt
